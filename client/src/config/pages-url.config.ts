class DASHBOARD {
	private root = '/dashboard'

	HOME = this.root
	WORKOUTS = `${this.root}/workouts`
	MEAL = `${this.root}/meal-plan`
	PROFILE = `${this.root}/profile`
	ONBOARDING = `/onboarding`
}

export const DASHBOARD_PAGES = new DASHBOARD()
